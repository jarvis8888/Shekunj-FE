import React from "react";
import "./index.scss";
import { withHeaderFooter } from "../../hocs/withHeaderFooter";

const index = () => {
  return <div>all success stories page</div>;
};

export default withHeaderFooter(index);
